spring:
  datasource:
    url: jdbc:postgresql://${DB_HOST:localhost}:${DB_PORT:5432}/${DB_NAME:nevis_search}
    username: ${DB_USER:nevis}
    password: ${DB_PASSWORD:nevis}
  ai:
    openai:
      api-key: ""
  flyway:
    clean-disabled: false
